export const MAX_IMAGE_PREVIEW_BYTES = 10 * 1024 * 1024;
export const MAX_IMAGE_PREVIEW_LABEL = "10 MB";

/** Bounded browser-preview ceiling; research-library PDFs larger than this need a future dedicated transport. */
export const MAX_PDF_PREVIEW_BYTES = 128 * 1024 * 1024;
export const MAX_PDF_PREVIEW_LABEL = "128 MB";

---
"@jmfederico/pi-web": patch
---

Add an opt-in synthetic Research workspace panel and paired Pi extension for bounded paper-context dispatch, scoped local search, citation/backlink previews, and additive answer drafts. Workspace file writes with `overwrite: false` now publish a complete temporary file through an exclusive destination link, preventing overwrite races and partial destination visibility.

---
"@jmfederico/pi-web": patch
---

Improve the model picker's All models view with an atomic Select all / Deselect all action. Select all clears model restrictions, while Deselect all keeps only the current model, which cannot be deactivated. Clicking an All models row now changes its availability without switching models or closing the dialog.

---
"@jmfederico/pi-web": patch
---

Fix the Updates tab disappearing, and the update command targeting the wrong package, when a Pi package is installed from a directory inside a local PI WEB checkout. Local checkouts are no longer misreported as Pi package installations.
